name: Deploy indexProductService to EC2

on:
  push:
    branches:
      - test2
    paths:
      - 'Backend/product_domain/searchService/indexProduct/**'
  pull_request:
    branches:
      - main
    paths:
      - 'Backend/product_domain/searchService/indexProduct/**'

jobs:
  test:
    name: Run Go Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.20'

      - name: Run Unit Tests
        working-directory: Backend/product_domain/searchService/indexProduct
        run: |
          go mod tidy
          go test ./...

  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build -t itsandy/search-service:index-product-latest ./Backend/product_domain/searchService/indexProduct

      - name: Push Docker image
        run: |
          docker push itsandy/search-service:index-product-latest

  deploy-qa:
    name: Deploy to QA (test2)
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/test2'

    steps:
      - name: Deploy to QA via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_PRODUCT_DOMAIN3_IP }}
          username: ubuntu
          key: ${{ secrets.EC2_PRODUCT_DOMAIN_PEM_KEY }}
          script: |
            cd ShoeCloud/Backend/product_domain/searchService/indexProduct
            docker pull itsandy/search-service:index-product-latest
            docker stop index-service || true
            docker rm index-service || true
            docker run -d --restart=always --name index-service -p 3017:8006 --env-file .env itsandy/search-service:index-product-latest

  deploy-prod:
    name: Deploy to Production (main)
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Deploy to Production via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_PRODUCT_DOMAIN3_PROD_IP }}
          username: ubuntu
          key: ${{ secrets.EC2_PRODUCT_DOMAIN_PEM_PROD_KEY }}
          script: |
            cd ShoeCloud/Backend/product_domain/searchService/indexProduct
            docker pull itsandy/search-service:index-product-latest
            docker stop index-service || true
            docker rm index-service || true
            docker run -d --restart=always --name index-service -p 3017:8006 --env-file .env itsandy/search-service:index-product-latest
