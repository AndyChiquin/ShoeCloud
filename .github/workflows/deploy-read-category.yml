name: Deploy readCategoryService to EC2

on:
  push:
    branches:
      - test2
    paths:
      - 'Backend/product_domain/categoryService/readCategory/**'
  pull_request:
    branches:
      - main
    paths:
      - 'Backend/product_domain/categoryService/readCategory/**'

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          cd Backend/product_domain/categoryService/readCategory
          npm install

      - name: Run Jest tests
        run: |
          cd Backend/product_domain/categoryService/readCategory
          npm test

  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build -t itsandy/category-service:read-category-latest ./Backend/product_domain/categoryService/readCategory

      - name: Push Docker image
        run: |
          docker push itsandy/category-service:read-category-latest

  deploy-qa:
    name: Deploy to QA (test2)
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/test2'

    steps:
      - name: Deploy to QA via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_PRODUCT_DOMAIN2_IP }}
          username: ubuntu
          key: ${{ secrets.EC2_PRODUCT_DOMAIN_PEM_KEY }}
          script: |
            cd ShoeCloud/Backend/product_domain/categoryService/readCategory
            docker pull itsandy/category-service:read-category-latest
            docker stop read-category-service || true
            docker rm read-category-service || true
            docker run -d --restart=always --name read-category-service -p 3009:3002 --env-file .env itsandy/category-service:read-category-latest

  deploy-prod:
    name: Deploy to Production (main)
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Deploy to Production via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_PRODUCT_DOMAIN2_PROD_IP }}
          username: ubuntu
          key: ${{ secrets.EC2_PRODUCT_DOMAIN_PEM_PROD_KEY }}
          script: |
            cd ShoeCloud/Backend/product_domain/categoryService/readCategory
            docker pull itsandy/category-service:read-category-latest
            docker stop read-category-service || true
            docker rm read-category-service || true
            docker run -d --restart=always --name read-category-service -p 3009:3002 --env-file .env itsandy/category-service:read-category-latest
