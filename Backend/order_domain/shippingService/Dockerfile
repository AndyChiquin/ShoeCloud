# Imagen base de Python
FROM python:3.10-slim

# Carpeta de trabajo
WORKDIR /app

# Copiar todos los archivos (solo para que sea válido)
COPY . .

# Comando dummy para evitar errores (no ejecuta app real)
CMD ["echo", "Dockerfile listo pero aún no se usa"]
