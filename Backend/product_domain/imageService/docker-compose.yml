version: '3.8'

services:
  image-service:
    build: .
    container_name: image-service
    ports:
      - "8007:8007"
    env_file:
      - .env
    depends_on:
      - mongo-image-service
    restart: always

  mongo-image-service:
    image: mongo:6
    container_name: mongo-image-service
    ports:
      - "27018:27017"
    volumes:
      - mongo_image_data:/data/db
    restart: always

volumes:
  mongo_image_data:
